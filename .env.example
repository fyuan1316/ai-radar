# 复制为 .env,填入真实值。.env 已被 gitignore。

# 飞书自建机器人 webhook
# 获取方式:飞书 → 创建一个只有你自己的群 → 群设置 → 群机器人 → 添加"自定义机器人" → 复制 webhook
FEISHU_WEBHOOK=https://open.feishu.cn/open-apis/bot/v2/hook/xxxxxxxx

# 可选:给 webhook 加签名校验时填
# FEISHU_SIGN_SECRET=

# GitHub 仓库(用于生成 digest 超链接)
# 格式:用户名/仓库名
GITHUB_REPO=fyuan1316/ai-radar

# GitHub API token(可选,强烈建议配)
# 用途:搜集 task 里用 curl 打 api.github.com,带 token 把速率上限从 60/h 提到 5000/h
# 最小权限:细粒度 PAT,public repo read 即可(不需要 repo 写权限)
# 不配的话也能跑,但每次周报 60 次 API 打满后会 429,只能分批跑+等 reset
# GITHUB_TOKEN=ghp_xxxxxxxxxxxxxxxx
